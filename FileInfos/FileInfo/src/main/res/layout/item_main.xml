<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="6dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="6dp">

    <ImageView
        android:id="@+id/item_main_iv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:scaleType="center"
        android:src="@drawable/file_info_dir" />

    <TextView
        android:id="@+id/item_main_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_toRightOf="@id/item_main_iv"
        android:text="内部储存"
        android:textSize="16dp" />

    <TextView
        android:id="@+id/item_main_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/item_main_name"
        android:layout_below="@id/item_main_name"
        android:layout_marginTop="4dp"
        android:text="总大小，计算大小，不可计算大小，可用大小"
        android:textSize="13dp" />
</RelativeLayout>
